# .env.sample for Heavenletters Backend

# Copy this file to .env in the backend directory and replace CHANGE_ME placeholders
# with actual secure values. This sample contains no real secrets.

# IMPORTANT: Never commit .env files to the repository. Ensure .env is listed in .gitignore.
# Distribute secrets via secure channels (e.g., password manager, vault) on a need-to-know basis.
# Rotate credentials upon initial setup, personnel changes, suspected leaks, or every 90 days.
# See docs/SECRETS_POLICY.md for detailed guidelines.

# ==============================================================================
# Database Configuration
# ==============================================================================

# Database connection string for Prisma/Keystone
# Format: mysql://USER:PASSWORD@HOST:PORT/DB?options
# Use least-privilege user (e.g., keystone_app) with access only to ks_* tables.
# IMPORTANT: Replace the following placeholders:
#   - CHANGE_ME: Replace with your actual database password
#   - YOUR_DB_HOST: Replace with actual database host (e.g., 192.168.8.103 for development)
DATABASE_URL=mysql://keystone_app:CHANGE_ME@YOUR_DB_HOST:3306/heaven?connection_limit=10

# ==============================================================================
# Application Configuration
# ==============================================================================

# KeystoneJS server port (default 3000)
# Change this for production deployments to avoid port conflicts
KEYSTONE_PORT=3000

# Allowed CORS origins (comma-separated for multiple; update for production domains)
# For development: typically your frontend development server (e.g., http://localhost:4321)
# For production: use your actual domain(s) (e.g., https://yourdomain.com)
KEYSTONE_CORS_ORIGIN=http://localhost:4321

# ==============================================================================
# Security Configuration
# ==============================================================================

# Secret key for sessions and authentication (generate a long, random string, e.g., 32+ chars)
# CRITICAL: This must be changed for production! Generate using: openssl rand -hex 32
# Never commit this value to version control
SESSION_SECRET=CHANGE_ME_LONG_RANDOM

# ==============================================================================
# Environment Configuration
# ==============================================================================

# Node.js environment (development, production, test)
# Controls application behavior, logging level, and error reporting
NODE_ENV=development